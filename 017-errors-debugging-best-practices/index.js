for (let num = 0; num < 10; num++) {
  if (10 % num == 0) { 
    console.log(`10 is evenly divisible by ${num}`);
  } 
}

//create a variable that holds the day of the week;
let dayOfWeek = "Monday";
// let day_of_week = "Monday";
const NUM_OF_DAYS = 7;